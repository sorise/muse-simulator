#ifndef MUSE_SIMULATOR_WORLD_USER_HPP
#define MUSE_SIMULATOR_WORLD_USER_HPP

namespace muse::simulator {
    /* @class world_user
     * @brief 模拟真实世界的用户请求，可以向模拟世界发送RPC请求。
     * @note 可以定时向某个主机发送请求，也可以定时但随机发送请求
     * @note 还可以一次性发送请求
     *
     * 1. 定时操作
     * */
    class world_user {

    };
}

#endif //MUSE_SIMULATOR_WORLD_USER_HPP
